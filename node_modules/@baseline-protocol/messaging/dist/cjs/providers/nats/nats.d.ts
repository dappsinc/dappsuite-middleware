import { NatsConfig } from './config';
import { IMessagingService } from '../..';
export declare class NatsService implements IMessagingService {
    private readonly config;
    private readonly getBearerToken;
    private service?;
    private bearerToken;
    constructor(config: NatsConfig, getBearerToken: () => Promise<string>);
    getSubscribedSubjects(): string[];
    connect(): Promise<void>;
    disconnect(): Promise<void>;
    isConnected(): boolean;
    publish(subject: string, data: any): Promise<void>;
    request(subject: string, timeout: number, data?: object): Promise<any>;
    subscribe(subject: string, callback: (msg: any, err?: any) => void): Promise<void>;
    unsubscribe(subject: string): Promise<void>;
    flush(): Promise<void>;
    private getSubjectsToUnsubscribeFrom;
}
//# sourceMappingURL=nats.d.ts.map